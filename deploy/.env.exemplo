# Repositorio que sera clonado no servidor
REPO_URL=https://github.com/seu-usuario/flux-generated.git
REPO_BRANCH=main

# Caminho persistente dentro do container de deploy
APP_DIR=/workspace/app

# Compose que sera executado dentro do repositorio clonado
COMPOSE_FILE=docker-compose.yml
COMPOSE_PROJECT_NAME=flux-generated
