# Aplicacao
BIND_ADDR=0.0.0.0:3000
APP_PORT=3000

# Banco
POSTGRES_DB=flux_generated
POSTGRES_USER=flux
POSTGRES_PASSWORD=flux
POSTGRES_PORT=5432

# Conexao usada pela aplicacao e pelo gerador
DATABASE_URL=postgres://flux:flux@db:5432/flux_generated
